import Routing from '@/pages';
import './styles/global.module.sass'

// Можно добавить метрики, логирование ошибок, обработку ошибок, и т.д.
const App = () => {
    return (
        <>
            <Routing />
        </>
    );
}

export default App;